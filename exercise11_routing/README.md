# Exercise 11 - Routing Analysis

This exercise investigates Linux routing behavior. The routing table is examined, and the path to a remote destination is traced using traceroute. The analysis includes hop-by-hop latency measurements and routing concepts.