# Exercise 09 - Firewall Configuration

This exercise configures a Linux firewall using UFW or IPTables. Security policies are implemented to allow only required services while blocking unnecessary incoming traffic. The configuration follows the principle of least privilege.