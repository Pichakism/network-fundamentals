# Exercise 07 - Port Accessibility Testing

This exercise tests network service accessibility by checking specific TCP ports. Common service ports such as SSH, HTTP, and MySQL are verified to determine whether they are reachable and accepting connections.